<link rel="stylesheet" href="css/conta.css">
<div class="background">
    <div class="circle" style="--tamanho:400px;--top:50%;"></div>
    <div class="circle" style="--tamanho:300px;--top:67%;animation-delay:0.5s;--left:15%;"></div>
</div>
<div class="main">
    <header>
        <div class="logo">
            <img src="/svg/logo.svg" alt="" srcset="">
        </div>
        <h2>dono</h2>
    </header>
    <div class="controles">
        <div id="saldo">
            <h3>Saldo disponivel</h3>
            <h1>R$ </h1>
        </div>
        <div class="controle">
            <a href="/transferencia">
            <div class="button">
                <div class="icon">
                    <img src="/svg/trasferencia.svg" alt="" srcset="">
                </div>
                <p>transferencia</p>
            </div>
            </a>
        </div>
    </div>
</div>
<script>
    const urlParams = new URLSearchParams(window.location.search);

const id = urlParams.get('jubtuisdpoiofr');
let conta ;
    {{#each contas}}
        if({{id}} == id){
            conta = {
                "dono":"{{dono}}",
                "saldo":"{{saldo}}",
                "senha":"{{senha}}"
            }

        }
    {{/each}}
    $('a').attr('href','/transferencia?c='+id);
</script>
<script src="/js/conta.js"></script>